from task_4_funcs import *

flag = True

#Здесь нужен принт с приветствием и подсказкой как начать работу в задании этот пункт прописан и
# также прописано что привествие не должно показываться дважды - по факту не выполнено ТЗ
if 'привет' in input().lower() and flag: ##Как пользователь узнает какое ключевое слово вводить для старта бота?

    print('Привет!')

    menu = {
        'наорать': up_print,
        'подсчитать символы': letters_count_print,
        'заменить': symbol_change_print,
        'список слов': words_from_string_print,
    }

    while flag:
        user_request = input(f'Вот, что я могу:\n'          ##Еще Раз прописано всё меню 
                             f'НАОРАТЬ\nПодсчитать символы\n'
                             f'Заменить\nCписок слов\nВыход\n'
                             f'Введите требуемое действие или вызовите оператора:\n')

        in_menu_condition = user_request.lower() in menu
        is_operator_condition = 'оператор' in user_request.lower()

        if user_request.lower() == 'выход':
            break
        elif is_operator_condition:
            print('Зову оператора!')
            break
        elif in_menu_condition:
            menu[user_request.lower()](input('Введите строку: '))


         ##Занимает слишком много места + если нужно будет добавить еще действия в бота, придется менять в 3ех местах
            ##Вообще неправильно использован цикл if - В первых двух условиях можно сделать брейк, а в двух оставшихся
            ##повторяется вывод - то есть его можно вынести из цикла

